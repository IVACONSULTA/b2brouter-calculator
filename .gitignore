node_modules

# Local Netlify folder
.netlify

# Environment variables — never commit real credentials
.env
.env.local
.env.*.local

# Supabase local state (generated by `supabase start`)
supabase/.temp
supabase/functions/.env

# Astro build output
dist

# Astro file-based sessions (local dev when NETLIFY_DEV=1)
.astro/session

# Editor / OS
.DS_Store
*.pem
